using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Pun;
using UnityEngine.UI;

public class ShowKillInfo : MonoBehaviour
{
    

    public static ShowKillInfo showKillInfoScrpt;
    // Start is called before the first frame update
    void Start()
    {
        showKillInfoScrpt = this;
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    
}
